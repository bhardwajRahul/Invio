name: invio

services:
  invio:
    image: ghcr.io/kittendevv/invio:latest
    env_file:
      - .env
    volumes:
      - invio_data:/app/data
    ports:
      - "8000:8000"
    restart: unless-stopped

volumes:
  invio_data:
    driver: local
