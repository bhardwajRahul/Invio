# Copy to .env at the repo root:
# cp .env.example .env

# ======================
# Backend configuration
# ======================
ADMIN_USER=admin
ADMIN_PASS=supersecret
JWT_SECRET=change-me-in-production

# SQLite database path
# - For Docker (recommended):
DATABASE_PATH=/app/data/invio.db
# - For local dev outside Docker, you could use:
# DATABASE_PATH=./invio.db

# Demo mode (optional): when true the app is writable but auto-resets from DEMO_DB_PATH
# at a fixed interval. Writes are allowed between resets.
# DEMO_MODE=false
# Optional path to a pristine demo database snapshot used for resets
# DEMO_DB_PATH=/app/data/invio-demo.db
# Interval hours between resets (default 0.5h)
# DEMO_RESET_HOURS=0.5
# Reset once on app start (default true)
# DEMO_RESET_ON_START=true

# ======================
# Networking / Ports
# ======================
# Host port mappings
BACKEND_PORT=3000
FRONTEND_PORT=8000

# Frontend internal container port (matches the app’s PORT)
FRONTEND_PORT_INTERNAL=8000

# ======================
# Frontend -> Backend URL
# ======================
# For Docker Compose (service name resolves on the network):
BACKEND_URL=http://backend:3000
# For local dev outside Docker, you might use:
# BACKEND_URL=http://localhost:3000